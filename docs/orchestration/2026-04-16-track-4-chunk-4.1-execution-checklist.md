# Track 4 Chunk 4.1 Execution Checklist

- [x] Read the authority docs in repo-defined order.
- [x] Identify the chunk objective, acceptance criteria, and bounded file scope.
- [x] Inspect the current compose and deployment surfaces for the missing app/worker/proxy path.
- [x] Confirm the chunk uses the existing in-process worker architecture instead of inventing a new worker service boundary.
- [x] Implement the supported single-host deployment shape in the allowed files only.
- [x] Align deployment docs and top-level guidance to the supported shape.
- [x] Run `docker compose config`.
- [x] Run `docker compose up -d postgres qdrant`.
- [x] Run `docker compose run --rm app saw seed-dev-data`.
- [x] Run `docker compose run --rm app saw verify-default-stack`.
- [x] Run `docker compose up -d --build app proxy`.
- [x] Run `python3 -c "import json, urllib.request; print(json.load(urllib.request.urlopen('http://127.0.0.1:8080/health/runtime'))['overall_status'])"`.
- [x] Run `python3 -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8080/workspace/').status)"`.
- [x] Run `rg -n "reverse proxy|single-host|docker compose|trusted team|supported deployment|health|verify" README.md docs/DEPLOYMENT.md .github/workflows/ci.yml docker-compose.yml infra`.
- [x] Prepare reviewer packet with allowed files, acceptance criteria, verification summary, and known non-goals.
- [x] Collect 3 reviewer decisions under read-only rules.
- [x] Address any findings without widening scope beyond Chunk 4.1.
- [x] Re-run required verification after any fix-up.
- [x] Mark the chunk signed off only after local verification plus reviewer consensus.

## Verification Summary

- Passed: `docker compose config`
- Passed: `docker compose up -d postgres qdrant`
- Passed: `docker compose run --rm app saw seed-dev-data`
- Passed: `docker compose run --rm app saw verify-default-stack`
- Passed after one image fix-up: `docker compose up -d --build app proxy`
- Passed: `python3 -c "import json, urllib.request; print(json.load(urllib.request.urlopen('http://127.0.0.1:8080/health/runtime'))['overall_status'])"` returned `ready`
- Passed: `python3 -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:8080/workspace/').status)"` returned `200`
- Passed: `rg -n "reverse proxy|single-host|docker compose|trusted team|supported deployment|health|verify" README.md docs/DEPLOYMENT.md .github/workflows/ci.yml docker-compose.yml infra`

## Reviewer Packet

- Allowed files:
  `docker-compose.yml`,
  `.github/workflows/ci.yml`,
  `README.md`,
  `docs/DEPLOYMENT.md`,
  bounded files under `infra/`,
  and the Track 4 Chunk 4.1 packet docs only
- Acceptance criteria:
  one deployment guide is real and tested; health and verification tooling
  match the guide; no unrelated runtime or auth scope is introduced
- Non-goals:
  no public-internet hardening, TLS/secret redesign, auth implementation, or
  multi-host/autoscaled deployment work

## Review Summary

- Review result: `3/3 approve`
- No blocking findings
