# Track 6 Chunk 6.1 Execution Checklist

- [x] Read the authority docs in repo-defined order.
- [x] Identify the chunk objective, acceptance criteria, and bounded file scope.
- [x] Choose one collaboration loop and reject broader workflow-system drift.
- [x] Inspect the current Bible route, workspace, and UI handoff seams.
- [x] Implement the bounded Bible handoff loop change in the allowed files only.
- [x] Narrow the packet to the actual write set and exclude adjacent untouched files.
- [x] Verify that workflow docs and README pointers match the selected loop.
- [x] Verify that permissions and role cues match the writer/operator boundary.
- [x] Verify that the UI clearly surfaces the handoff.
- [x] Run the bounded verification commands for this chunk.
- [x] Prepare reviewer packet with allowed files, acceptance criteria,
  verification summary, and known non-goals.
- [x] Collect 3 reviewer decisions under read-only rules.
- [x] Address any findings without widening scope beyond Chunk 6.1.
- [x] Re-run required verification after any fix-up.
- [x] Mark the chunk signed off only after local verification plus reviewer
  consensus.

## Verification Summary

- Passed: `node --check frontend/operator-ui/app.js`
- Passed: `.venv/bin/pytest -q tests/test_api_integration.py -k 'bible or workspace'`
- Passed: `.venv/bin/pytest -q tests/test_api_integration.py tests/test_health.py -k 'workspace or bible or auth'`
- Passed: `.venv/bin/pytest -q`

## Review Summary

- First review pass: `0 approve`, `3 request changes`
- Blocking findings:
  - operator-only actions still surfaced too early in the UI handoff state
  - workspace messaging still implied a second explicit collaboration loop
  - workflow docs mixed `/workspace/` and `/operator/` at the operator step
  - export verification did not yet prove the manual-edited text survived
- First revision:
  - aligned the workflow doc and README pointer with one Bible handoff loop
  - refreshed workspace summary after writer profile and section saves
  - preserved the operator handoff cue even when another section stayed thin
  - expanded the integration scenario to prove exported manual text survives
- Second review pass: `1 approve`, `2 request changes`
- Blocking finding:
  - fallback UI handoff cues still keyed off manual edits alone instead of the
    full operator-ready condition
- Second revision:
  - tightened the fallback workspace and Bible-screen handoff state to require
    both `has_manual_edits` and `ready_for_writer`
  - added an explicit thin-section regression proof around the surviving
    operator handoff cue
- Reverification after fix-up passed:
  - `node --check frontend/operator-ui/app.js`
  - `.venv/bin/pytest -q tests/test_api_integration.py -k 'bible or workspace'`
  - `.venv/bin/pytest -q`
- Final rereview reached `3/3 approve`
