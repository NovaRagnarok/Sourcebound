# Track 1 Chunk 1.0 Task Brief

## Chunk Identity

- Track id: `Track 1`
- Chunk id: `Chunk 1.0`
- Chunk title: `Green Baseline`
- Backlog id: `SB-001`

## Task Brief

Restore the repo to a green baseline by fixing the Zotero adapter write-path contract mismatch. Keep the chunk tightly bounded to the adapter write path and its tests. Do not relax live-write safety checks just to satisfy unit tests. The mocked-path behavior must be explicit in code and tests.

## Explicit Ownership

- Implementation owner:
  `tests/test_adapters.py` and `tests/test_api_integration.py` for deterministic contract fixes, with `src/source_aware_worldbuilding/adapters/zotero_adapter.py` preserved unless a narrower-safe code change becomes necessary
- Verification owner:
  targeted local checks centered on `tests/test_adapters.py` and repo-wide `pytest`
- Review-prep owner:
  this chunk packet, allowed-file boundary checks, acceptance criteria summary, and reviewer prompts

## Allowed Files

- `src/source_aware_worldbuilding/adapters/zotero_adapter.py`
- `tests/test_adapters.py`
- `tests/test_api_integration.py`
- `tests/test_cli_intake.py` only if required by the final bounded fix
- `docs/orchestration/2026-04-16-track-1-chunk-1.0-implementation-plan.md`
- `docs/orchestration/2026-04-16-track-1-chunk-1.0-execution-checklist.md`
- `docs/orchestration/2026-04-16-track-1-chunk-1.0-task-brief.md`

## Acceptance Criteria

- Full local `pytest` is green.
- The Zotero write path has explicit coverage for credential-gated live behavior.
- The Zotero write path has explicit coverage for mocked-path behavior without relying on ambient credentials.
- The setup-surface API test explicitly simulates unavailable default services instead of depending on host environment state.
- No unrelated files are edited.

## Verification Commands

- `.venv/bin/pytest -q tests/test_adapters.py`
- `.venv/bin/pytest -q tests/test_cli_intake.py`
- `.venv/bin/pytest -q`

## Non-Goals

- No work on `Chunk 1.1` diagnostics or later backlog items.
- No repo-wide test cleanup unrelated to the Zotero write path.
- No unrelated data file or dev fixture changes.
- No redesign of the adapter API or broader Zotero intake behavior beyond making the write-path contract explicit.
