# Track 5 Chunk 5.3 Execution Checklist

- [x] Read the authority docs in repo-defined order.
- [x] Identify the chunk objective, acceptance criteria, and bounded file scope.
- [x] Inspect the current deployment guide, compose stack, and infra assets for one bounded supported internet-facing shape.
- [x] Tighten the packet to the exact allowed workflow file and the HTTPS/auth proof path.
- [x] Implement the bounded deployment-hardening change in the allowed files only.
- [x] Exercise the supported deployment end to end at least once through the public proxy.
- [x] Document TLS, auth, and secret-handling guidance in `docs/DEPLOYMENT.md`.
- [x] Run the bounded deployment verification commands for this chunk.
- [x] Prepare reviewer packet with allowed files, acceptance criteria, verification summary, and known non-goals.
- [x] Collect 3 reviewer decisions under read-only rules.
- [x] Address any findings without widening scope beyond Chunk 5.3.
- [x] Re-run required verification after any fix-up.
- [x] Mark the chunk signed off only after local verification plus reviewer consensus.

## Verification Summary

- Passed `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml config`
- Passed bounded local proof with temporary TLS material and trusted-team tokens:
  - `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml up -d --build postgres qdrant`
  - `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml run --rm app saw seed-dev-data`
  - `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml run --rm app saw verify-default-stack`
  - `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml up -d --build app proxy`
  - HTTPS proxy health reached `ready`
  - HTTPS `/workspace/` returned `200`
  - HTTP `/workspace/` redirected to the configured HTTPS host and port
  - authenticated operator `POST /v1/intake/text` through the HTTPS proxy returned `200` with a `created_item` payload

## Review Summary

- First review pass: `2 approve`, `1 request changes`
- Blocking finding:
  - the HTTP-to-HTTPS redirect trusted the inbound host and ignored non-default
    HTTPS ports
- Revision:
  - made the public host explicit with `SOURCEBOUND_PUBLIC_HOST`
  - updated the nginx internet-facing config to redirect to the configured host
    and HTTPS port
  - changed the proxy startup path to render and run the full internet-facing
    nginx config from `/tmp`, avoiding the base read-only `default.conf` mount
  - updated docs and CI to verify the configured redirect target
- Reverification after fix-up passed with an isolated Compose project
- Rereview reached `3/3 approve`
