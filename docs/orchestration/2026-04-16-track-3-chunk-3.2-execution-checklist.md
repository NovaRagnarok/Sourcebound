# Track 3 Chunk 3.2 Execution Checklist

- [x] Read the authority docs in repo-defined order.
- [x] Identify the chunk objective, acceptance criteria, and bounded file scope.
- [x] Inspect the current extraction-eval artifact and CI surfaces with local evidence.
- [x] Confirm the packet allows any focused test coverage needed for stable artifact output.
- [x] Implement bounded retrieval-regression-gate changes in the allowed files only.
- [x] Add or update focused tests for stable benchmark output or retrieval-gate behavior.
- [x] Run `.venv/bin/pytest -q tests/test_extraction_eval.py tests/test_query_service.py`.
- [x] Run `.venv/bin/saw evaluate-extraction --dataset all --json-output`.
- [x] Run `.venv/bin/pytest -q`.
- [x] Prepare reviewer packet with allowed files, acceptance criteria, verification summary, and known non-goals.
- [x] Collect 3 reviewer decisions under read-only rules.
- [x] Address any findings without widening scope beyond Chunk 3.2.
- [x] Re-run required verification after any fix-up.
- [x] Mark the chunk signed off only after local verification plus reviewer consensus.

## Verification Summary

- Passed: `.venv/bin/pytest -q tests/test_extraction_eval.py tests/test_query_service.py`
- Passed: `.venv/bin/saw evaluate-extraction --dataset all --json-output`
- Passed: `.venv/bin/pytest -q`
- Re-run after review fix-up: all three commands passed again after aligning CI artifact upload with the documented failure-triage flow

## Reviewer Packet

- Allowed files:
  `src/source_aware_worldbuilding/services/query.py`,
  `src/source_aware_worldbuilding/extraction_eval.py`,
  `tests/test_extraction_eval.py`,
  `.github/workflows/ci.yml`,
  `docs/EXTRACTION_EVAL.md`,
  and the Track 3 Chunk 3.2 packet docs only
- Acceptance criteria:
  benchmark summary output is stable and diff-friendly; retrieval regression visibility exists in automation; no unrelated files are edited
- Non-goals:
  no broader benchmark redesign, no unrelated CI expansion, no deployment/job/auth/UI drift

## Review Summary

- Initial review: `1 approve`, `2 request changes`
- Findings addressed inside Chunk 3.2 only by aligning CI artifact upload with the documented failure-triage path
- Rereview: `3/3 approve`
