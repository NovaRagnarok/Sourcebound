# Track 5 Chunk 5.3 Task Brief

## Chunk Identity

- Track id: `Track 5`
- Chunk id: `Chunk 5.3`
- Chunk title: `Hardened Internet-Facing Deployment`
- Backlog id: `SB-020`

## Task Brief

Validate one supported internet-facing deployment shape for a small trusted
team. This chunk should make the deployment guide explicit about TLS, auth, and
secret handling for that one shape and prove the path end to end at least once.

## Explicit Ownership

- Implementation owner:
  `docs/DEPLOYMENT.md`,
  `infra/`,
  `docker-compose.yml`,
  and any bounded workflow proof under `.github/workflows/`
- Verification owner:
  the chosen end-to-end deployment proof and regression checks that stay inside
  the deployment slice
- Review-prep owner:
  this chunk packet, allowed-file boundary checks, acceptance-criteria summary,
  and reviewer prompts

## Allowed Files

- `docs/DEPLOYMENT.md`
- `infra/`
- `docker-compose.yml`
- `.github/workflows/ci.yml` only for the bounded `internet-facing-compose`
  proof job
- `docs/orchestration/2026-04-16-track-5-chunk-5.3-implementation-plan.md`
- `docs/orchestration/2026-04-16-track-5-chunk-5.3-execution-checklist.md`
- `docs/orchestration/2026-04-16-track-5-chunk-5.3-task-brief.md`

## Acceptance Criteria

- deployment guide is explicit about one supported TLS-terminated
  internet-facing shape, including writer/operator token handling and secret
  loading expectations
- the supported deployment is exercised end to end at least once through the
  public proxy, including one authenticated operator mutation

## Verification Commands

- `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml config`
- `docker compose -f docker-compose.yml -f infra/compose/internet-facing.yml up -d --build postgres qdrant app proxy`
- the bounded HTTPS health, workspace, and authenticated operator-intake proof
  for the internet-facing path

## Non-Goals

- no broad multi-host or autoscaled deployment architecture
- no public sign-up or tenant provisioning work
- no release-automation redesign beyond the bounded deployment proof
- no identity-provider, session, or per-user account work
- no unrelated app feature work outside deployment support
