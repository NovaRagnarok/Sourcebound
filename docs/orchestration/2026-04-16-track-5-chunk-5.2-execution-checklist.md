# Track 5 Chunk 5.2 Execution Checklist

- [x] Read the authority docs in repo-defined order.
- [x] Identify the chunk objective, acceptance criteria, and bounded file scope.
- [x] Inspect the current storage, Postgres-backed projection, and CLI rebuild paths.
- [x] Implement the bounded upgrade-safety change in the allowed files only.
- [x] Add or update at least one upgrade-path verification.
- [x] Document the supported upgrade steps in `docs/DEPLOYMENT.md`.
- [x] Run `.venv/bin/pytest -q tests/test_cli_qdrant.py tests/test_postgres_integration.py -k 'qdrant or rebuild or upgrade or projection'`.
- [x] Run `.venv/bin/pytest -q`.
- [x] Prepare reviewer packet with allowed files, acceptance criteria, verification summary, and known non-goals.
- [x] Collect 3 reviewer decisions under read-only rules.
- [x] Address any findings without widening scope beyond Chunk 5.2.
- [x] Re-run required verification after any fix-up.
- [x] Mark the chunk signed off only after local verification plus reviewer consensus.

## Verification Summary

- `.venv/bin/pytest -q tests/test_cli_qdrant.py tests/test_postgres_integration.py -k 'qdrant or rebuild or upgrade or projection'` passed.
- `.venv/bin/saw upgrade-check --json-output` passed.
- `.venv/bin/pytest -q` passed.

## Review Summary

- Reviewer 1: `APPROVE`
- Reviewer 2: `APPROVE`
- Reviewer 3: `APPROVE`
- Revision resolved inside the same chunk:
  `upgrade-check` is now read-only, the uninitialized-schema regression is covered, and the Qdrant deployment wording now matches the actual refresh/backfill guarantee.
